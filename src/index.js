import { Gameboard } from './factories/Gameboard.js';

const renderShip = (length, xLoc, yLoc) => {
  console.log('length');
};

let test = Gameboard();
test.receiveAttack();

console.log('hello');

export { renderShip };
